name: fiction-ping
runtime:
  name: python
  options:
    virtualenv: venv
description: serverless mangadex feed pinger for discord
config:
  pulumi:tags:
    value:
      pulumi:template: serverless-aws-python
